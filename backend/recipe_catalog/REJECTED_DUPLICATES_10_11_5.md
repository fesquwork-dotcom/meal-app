# Rejected duplicate concepts — Sprint 10.11.5

- **Turkey + couscous dinner:** rejected because `turkey-couscous-lunch` already covers turkey + couscous; dinner twin would fail duplicate heuristics.
- **Chickpea + tomato + spinach dinner ≤20:** rejected because `chickpea-spinach-dinner` already exists (25 min batch/leftover); accelerating the same structure is not a new concept.
- **Faster fish + vegetable skillet:** rejected because `fish-veg-skillet-dinner` already owns that pattern; new card uses fish + beans + cabbage instead.
- **Tuna + zucchini batch rewrite:** rejected because `tuna-zucchini-dinner` already exists at 20 minutes (not batch_friendly); rewriting flags alone without new structure is disallowed.
- **Chicken + mushroom ≤20:** rejected because `chicken-mushroom-dinner` already covers chicken + mushrooms batch/leftover.
- **Beef + zucchini ≤20:** rejected because `beef-zucchini-dinner` already covers that skillet pattern; new beef card is bean-forward chilli.
